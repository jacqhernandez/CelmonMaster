<h1> Welcome to the Trading Centre </h1>

<table>
<% @users.each do |user| %>
<tr>
  <td><%= user.email %><td>
  <td><%= link_to "Show monsters", show_other_users_path(user.id) %><td>
  <td><%= link_to "Send Request", game_path(current_user.id) %></td>
</tr>
<% end %>
</table>
<br>
<br>
<p>add SEND REQUEST LINK per user. redirects to a page showing current_user.monsters and trade button. when trade button is clicked, request to user is sent. may status dapat yung request (like pending, approved)
kaso dapat maremember yung pinanggalingan user na gusto makipag-trade ng current user</td>

