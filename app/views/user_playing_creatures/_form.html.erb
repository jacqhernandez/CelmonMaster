<%= simple_nested_form_for(@user_playing_creature) do |f| %>

  <div class="form-inputs">
    <%= f.input :user_creature_id, :collection => @user_creatures %>
    <br>
  </div>

<div class="form-actions">
    <%= f.button :submit %>
  </div>
  
<% end %>
